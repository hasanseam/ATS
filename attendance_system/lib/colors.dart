import 'package:flutter/material.dart';

class AppColors {
  // Primary color - Oxford Blue
  static const Color primaryColor = Color(0xFF0E1B32); // Oxford Blue

  // Secondary color - blend well with Oxford Blue
  static const Color secondaryColor = Color(0xFF4E6EAF); // Cadet Blue (a lighter shade of blue)

  // Background color - light neutral for readability
  static const Color backgroundColor = Color(0xFFF4F6FA); // Light Grayish Blue

  // Additional colors
  static const Color accentColor = Color(0xFFF5A623); // Warm accent color (Amber)
  static const Color textPrimaryColor = Color(0xFFFFFFFF); // White for text on dark backgrounds
  static const Color textSecondaryColor = Color(0xFFB0BEC5); // Light Gray for secondary text
}